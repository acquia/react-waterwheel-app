# Waterwheel React

```
npm i
```

Create a config file, `src/config.js`.

```js
module.exports = {
  waterwheel: {
    base: 'http://foo.dev',
    credentials: {
      oauth: 'my-oauth-token'
    }
  }
};
```

```
npm run dev
```
